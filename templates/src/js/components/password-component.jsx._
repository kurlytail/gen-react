<%
const cell = design.MasterCell.find(cell => cell.name === context);
const className = lodash.upperFirst(lodash.camelCase(cell.name));
%>
import React, { Component } from 'react';
import PropTypes from 'prop-types';
import lodash from 'lodash';

class <%=className%> extends Component {
    render() {
        return <div <%=extension('react-tag.*', 'input-div-attributes').join(' ')%>>
            <label htmlFor={this.props.label} <%=extension('react-tag.*', 'input-label-attributes').join(' ')%>>
                {lodash.startCase(this.props.label)}
            </label>
            <input type={'password'} name={this.props.label} <%=extension('react-tag.*', 'input-attributes').join(' ')%>/>
        </div>
    }
}

<%=className%>.propTypes = {
    label: PropTypes.string
};

export default <%=className%>;