<%
const cell = design.MasterCell.find(cell => cell.name === context);
const className = lodash.upperFirst(lodash.camelCase(cell.name));
%>
<%=extension('react-component-import-.*').join('')%>

class <%=className%> extends Component {
    render() {
        return <div <%=extension('react-tag.*', 'email-div-attributes').join(' ')%>>
            <label htmlFor={this.props.label} <%=extension('react-tag.*', 'email-label-attributes').join(' ')%>>
                {lodash.startCase(this.props.label)}
            </label>
            <input type={'email'} name={this.props.label} <%=extension('react-tag.*', 'email-attributes').join(' ')%>/>
        </div>
    }
}

<%=className%>.propTypes = {
    label: PropTypes.string
};

export default <%=className%>;